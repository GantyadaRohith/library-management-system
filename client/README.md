# Client (React)

This folder contains the React frontend for the Library Management System.

## Setup
1. Run `npm install` to install dependencies
2. Run `npm start` to launch the development server
